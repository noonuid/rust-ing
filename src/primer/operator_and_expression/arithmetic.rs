#[cfg(test)]
mod tests {
    #[test]
    fn example_arithmetic() {
        println!("8 + 2 = {}", 8u32 + 2);
        println!("8 - 2 = {}", 8u32 - 2);
        println!("8 * 2 = {}", 8u32 * 2);
        println!("8 / 2 = {}", 8u32 / 2);
    }
}
